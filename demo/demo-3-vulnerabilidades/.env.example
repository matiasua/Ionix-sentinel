# Ejemplo de variables de entorno (uso correcto). El repo contiene a propósito
# violaciones donde credenciales/keys están hardcodeadas en el código en vez
# de leerse de aquí — ver README.md, sección "Catálogo de vulnerabilidades".
DATABASE_URL=postgres://app_user:CHANGE_ME@localhost:5432/ionix_payments
PAN_ENCRYPTION_KEY=
STRIPE_SECRET_KEY=
JWT_SECRET=
